import { jobSchema } from "./sources/jobs";
import { partSchema } from "./sources/parts";
import { uploadSchema } from "./sources/uploads";

const schemas = [jobSchema, partSchema, uploadSchema];

export default schemas;
